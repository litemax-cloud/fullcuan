# Contributing to bot_masterslot333

- Branch naming: `feature/<name>`, `hotfix/<name>`
- Run tests: `pytest`
- Use PRs. Provide description and tests for parser changes.
- Do not commit secrets; use `.env` and repo secrets on deploy platform.
